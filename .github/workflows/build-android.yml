name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. إعداد Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 2. تثبيت المكتبات
      - name: Install Dependencies
        run: |
          npm install --legacy-peer-deps
          npm install @capacitor/assets --save-dev --legacy-peer-deps
          npm install @capacitor/app-launcher --save --legacy-peer-deps

      # =========================================================
      # 3. الحل الجذري: استخدام Intent Video Force
      # =========================================================
      - name: Inject Intent Video Logic
        run: |
          # 1. تحديد مكان مجلد assets
          ASSETS_DIR=$(find . -type d -name "assets" | head -n 1)
          if [ -z "$ASSETS_DIR" ]; then
            mkdir -p www/assets
            ASSETS_DIR="www/assets"
          fi
          echo "📂 Assets directory found at: $ASSETS_DIR"

          # 2. كتابة كود الجافاسكريبت (الخاص بالـ Intent)
          cat << 'EOF' > "$ASSETS_DIR/player_fix.js"
          
          console.log("🔧 Video Intent Script Loaded");

          document.addEventListener("DOMContentLoaded", () => {
              // ننتظر قليلاً حتى يتم تحميل المشغل
              setTimeout(initExternalPlayerFix, 2500);
          });

          function initExternalPlayerFix() {
              const btn = document.getElementById('btn-open-external');
              
              if (!btn) {
                  setTimeout(initExternalPlayerFix, 1000);
                  return;
              }

              // استنساخ الزر لمسح أي وظائف قديمة لا تعمل
              const newBtn = btn.cloneNode(true);
              btn.parentNode.replaceChild(newBtn, btn);

              // إضافة الوظيفة الجديدة
              newBtn.addEventListener('click', async (e) => {
                  e.preventDefault();
                  e.stopPropagation();

                  // 1. الحصول على الرابط من الفيديو الشغال حالياً
                  const videoTag = document.getElementById('videoPlayer');
                  let videoUrl = null;

                  if (videoTag && videoTag.src && videoTag.src.startsWith('http')) {
                      videoUrl = videoTag.src;
                  } else if (typeof currentStreamInfo !== 'undefined' && currentStreamInfo.url) {
                      videoUrl = currentStreamInfo.url;
                  }

                  if (!videoUrl) {
                      alert("⚠️ الرابط غير جاهز، انتظر تشغيل الفيديو وحاول مرة أخرى.");
                      return;
                  }

                  // 2. تحويل الرابط إلى Intent (هذا هو الحل السحري)
                  // هذا يجبر النظام على تجاهل المتصفح وفتح مشغل فيديو
                  const scheme = videoUrl.startsWith('https') ? 'https' : 'http';
                  const cleanUrl = videoUrl.replace(scheme + '://', '');
                  
                  // الصيغة: intent://رابط#Intent;scheme=http;type=video/*;end
                  const intentUrl = 'intent://' + cleanUrl + '#Intent;scheme=' + scheme + ';type=video/*;action=android.intent.action.VIEW;end';

                  const isAndroid = /Android/i.test(navigator.userAgent);
                  
                  if (isAndroid && window.Capacitor) {
                      try {
                          console.log("Launching Intent via AppLauncher: " + intentUrl);
                          await Capacitor.Plugins.AppLauncher.openUrl({ url: intentUrl });
                      } catch (err) {
                          console.warn("AppLauncher failed, using location fallback");
                          // خطة بديلة قوية جداً: تغيير موقع النافذة إلى الـ Intent
                          // المتصفح لا يستطيع فتح intent لذلك سيحوله للنظام فوراً
                          window.location.href = intentUrl;
                      }
                  } else {
                      // للكمبيوتر
                      window.open(videoUrl, '_blank');
                  }
              });
              
              // تغيير لون الحدود للأزرق للتأكد من تفعيل الكود
              newBtn.style.border = "2px solid #00a8ff"; 
              console.log("✅ External Player Button is Ready!");
          }
          EOF

          # 3. ربط الملف بـ index.html
          INDEX_FILE=$(find . -name "index.html" -type f | head -n 1)
          
          node -e "
            const fs = require('fs');
            const file = '$INDEX_FILE';
            let content = fs.readFileSync(file, 'utf8');
            const scriptTag = '<script src=\"assets/player_fix.js\"></script></body>';
            
            // نتأكد من عدم التكرار
            if (!content.includes('player_fix.js')) {
                content = content.replace('</body>', scriptTag);
                fs.writeFileSync(file, content);
                console.log('✅ Script injected into index.html');
            }
          "

      # 4. مزامنة إضافات Capacitor
      - name: Sync Capacitor Plugins
        run: |
          npx cap add android || true
          npx cap sync android

      # 5. توليد الأيقونات
      - name: Generate Icons & Splash
        run: |
          if [ -f "assets/icon.png" ]; then npx @capacitor/assets generate --android; fi

      # 6. إضافة الأذونات + استعلامات الفيديو (مهم جداً لنجاح الـ Intent)
      - name: Inject Permissions & Queries
        run: |
          # 1. السماح بـ HTTP (لحل مشكلة الشاشة البيضاء والتحذير الأمني)
          sed -i 's/<application/<application android:usesCleartextTraffic="true"/g' android/app/src/main/AndroidManifest.xml
          
          # 2. أذونات إضافية
          sed -i '/<application/i <uses-permission android:name="android.permission.WRITE_SETTINGS" />' android/app/src/main/AndroidManifest.xml
          sed -i '/<application/i <uses-permission android:name="android.permission.INTERNET" />' android/app/src/main/AndroidManifest.xml

          # 3. السماح برؤية تطبيقات الفيديو (بدون هذا السطر الـ Intent سيفشل)
          sed -i '/<application/i <queries><intent><action android:name="android.intent.action.VIEW" /><data android:mimeType="video/*" /></intent><package android:name="org.videolan.vlc" /><package android:name="com.mxtech.videoplayer.ad" /><package android:name="com.mxtech.videoplayer.pro" /></queries>' android/app/src/main/AndroidManifest.xml

      # 7. إعداد Java
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # 8. بناء ملف APK
      - name: Build APK with Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      # 9. إعادة التسمية
      - name: Rename APK
        run: |
          mv android/app/build/outputs/apk/debug/app-debug.apk android/app/build/outputs/apk/debug/IPTV-Elpop.apk

      # 10. الرفع
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: IPTV-Elpop
          path: android/app/build/outputs/apk/debug/IPTV-Elpop.apk
