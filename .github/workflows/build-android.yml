name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. إعداد Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 2. تثبيت المكتبات
      - name: Install Dependencies
        run: |
          npm install --legacy-peer-deps
          npm install @capacitor/assets --save-dev --legacy-peer-deps
          npm install @capacitor/app-launcher --save --legacy-peer-deps

      # =========================================================
      # 3. الحل الجذري: كشف المتغيرات المخفية + إصلاح الزر
      # =========================================================
      - name: Apply Final Player Fix
        run: |
          # 1. تحديد مكان مجلد assets
          ASSETS_DIR=$(find . -type d -name "assets" | head -n 1)
          if [ -z "$ASSETS_DIR" ]; then
            mkdir -p www/assets
            ASSETS_DIR="www/assets"
          fi
          
          # 2. إنشاء ملف سكربت المشغل (player_fix.js)
          cat << 'EOF' > "$ASSETS_DIR/player_fix.js"
          
          console.log("🔧 Final Fix Script Loaded");

          document.addEventListener("DOMContentLoaded", () => {
              // ننتظر قليلاً للتأكد من تحميل كل شيء
              setTimeout(hookExternalPlayer, 3000);
          });

          function hookExternalPlayer() {
              const btn = document.getElementById('btn-open-external');
              if (!btn) {
                  setTimeout(hookExternalPlayer, 1000);
                  return;
              }

              console.log("✅ Button found. Applying logic...");
              
              // استنساخ الزر لتنظيفه من أي كود قديم
              const newBtn = btn.cloneNode(true);
              btn.parentNode.replaceChild(newBtn, btn);

              // إضافة حدود حمراء للتأكد من التفعيل
              newBtn.style.border = "2px solid red"; 
              
              newBtn.addEventListener('click', async (e) => {
                  e.preventDefault();
                  e.stopPropagation();

                  // تنبيه للتأكد أن الزر يستجيب
                  // alert("جاري جلب الرابط..."); 

                  let finalUrl = null;

                  // المحاولة 1: المتغير العام (الذي قمنا بكشفه)
                  if (window.currentStreamInfo && window.currentStreamInfo.url) {
                      finalUrl = window.currentStreamInfo.url;
                      console.log("Got URL from window.currentStreamInfo");
                  } 
                  // المحاولة 2: HLS (الذي قمنا بكشفه)
                  else if (window.hls && window.hls.url) {
                      finalUrl = window.hls.url;
                      console.log("Got URL from window.hls");
                  }
                  // المحاولة 3: عنصر الفيديو (مع استبعاد blob)
                  else {
                      const v = document.getElementById('videoPlayer');
                      if (v && v.src && !v.src.startsWith('blob:')) {
                          finalUrl = v.src;
                      }
                  }

                  if (!finalUrl) {
                      alert("⚠️ لم يتم العثور على رابط صالح!\nتأكد أن الفيديو يعمل.\n(StreamInfo is empty)");
                      return;
                  }

                  // تنظيف الرابط
                  const scheme = finalUrl.startsWith('https') ? 'https' : 'http';
                  const cleanUrl = finalUrl.replace(scheme + '://', '');
                  
                  // بناء الـ Intent
                  const intentUrl = 'intent://' + cleanUrl + '#Intent;scheme=' + scheme + ';type=video/*;action=android.intent.action.VIEW;end';

                  const isAndroid = /Android/i.test(navigator.userAgent);

                  if (isAndroid && window.Capacitor) {
                      try {
                          await Capacitor.Plugins.AppLauncher.openUrl({ url: intentUrl });
                      } catch (err) {
                          console.error(err);
                          // محاولة أخيرة عبر المتصفح
                          window.location.href = intentUrl;
                      }
                  } else {
                      window.open(finalUrl, '_blank');
                  }
              });
          }
          EOF

          # 3. تعديل index.html لكشف المتغيرات وحقن السكربت
          # سنستخدم Node.js للقيام بالتعديلات بدقة لتجنب أخطاء النصوص
          INDEX_FILE=$(find . -name "index.html" -type f | head -n 1)
          
          node -e "
            const fs = require('fs');
            const file = '$INDEX_FILE';
            let content = fs.readFileSync(file, 'utf8');
            
            // 1. تحويل المتغيرات المخفية (let) إلى عامة (window.)
            // نستبدل 'let currentStreamInfo' بـ 'window.currentStreamInfo'
            content = content.replace(/let\s+currentStreamInfo\s*=/g, 'window.currentStreamInfo =');
            
            // نستبدل 'let hls' بـ 'window.hls'
            content = content.replace(/let\s+hls\s*=/g, 'window.hls =');

            // 2. حقن السكربت في النهاية
            const scriptTag = '<script src=\"assets/player_fix.js\"></script></body>';
            if (!content.includes('player_fix.js')) {
                content = content.replace('</body>', scriptTag);
            }
            
            fs.writeFileSync(file, content);
            console.log('✅ index.html patched: Variables exposed & script injected.');
          "

      # 4. مزامنة إضافات Capacitor
      - name: Sync Capacitor Plugins
        run: |
          npx cap add android || true
          npx cap sync android

      # 5. توليد الأيقونات
      - name: Generate Icons & Splash
        run: |
          if [ -f "assets/icon.png" ]; then npx @capacitor/assets generate --android; fi

      # 6. إضافة الأذونات
      - name: Inject Permissions & Queries
        run: |
          sed -i 's/<application/<application android:usesCleartextTraffic="true"/g' android/app/src/main/AndroidManifest.xml
          sed -i '/<application/i <uses-permission android:name="android.permission.WRITE_SETTINGS" />' android/app/src/main/AndroidManifest.xml
          sed -i '/<application/i <uses-permission android:name="android.permission.INTERNET" />' android/app/src/main/AndroidManifest.xml
          sed -i '/<application/i <queries><intent><action android:name="android.intent.action.VIEW" /><data android:mimeType="video/*" /></intent><package android:name="org.videolan.vlc" /><package android:name="com.mxtech.videoplayer.ad" /><package android:name="com.mxtech.videoplayer.pro" /></queries>' android/app/src/main/AndroidManifest.xml

      # 7. إعداد Java
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # 8. بناء ملف APK
      - name: Build APK with Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      # 9. إعادة التسمية
      - name: Rename APK
        run: |
          mv android/app/build/outputs/apk/debug/app-debug.apk android/app/build/outputs/apk/debug/IPTV-Elpop.apk

      # 10. الرفع
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: IPTV-Elpop
          path: android/app/build/outputs/apk/debug/IPTV-Elpop.apk
